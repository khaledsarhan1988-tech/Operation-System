# CLAUDE.md — دستور نظام «أكاديمية أحمد حسن» (نظام إدارة التشغيل)

> **اقرأ هذا الملف بالكامل قبل أي إضافة أو تعديل أو إجابة عن أي تقرير.**
> هذا هو **مصدر الحقيقة** للقرارات وطرق الحساب والعرض المتفق عليها حتى **2026-06-06**.

---

## ⚠️ 0) قواعد إلزامية (اقرأها أولًا)

1. **ممنوع أي تعديل كود من غير موافقة صريحة من صاحب النظام (Owner) أولًا.**
2. **التحقّق من البيانات الحية فقط — ممنوع نهائيًا الاعتماد على ملفات النسخ الاحتياطية** (`Quality_System_Data/backub/*.db`) لاتخاذ أي قرار أو إثبات أي رقم. النسخ قديمة وضلّلت التشخيص أكثر من مرة. استخدم دائمًا snapshot حيّ (القسم 1).
3. **التحقّق بالـ endpoint الحقيقي، لا بأي «نسخة يدوية» (hand-mirror) من المنطق.** المرايا اليدوية تنحرف وتخفي الأخطاء. شغّل الكود الفعلي (القسم 1).
4. **كشف التعارض (مهم):** قبل تنفيذ أي تعديل، طابقه على «**القرارات المتفق عليها**» (القسم 3). لو التعديل المطلوب يخالف قرارًا/طريقة حساب موثّقة هنا، **قف وقُل للمستخدم:**
   > «التعديل ده بيتعارض مع السيستم المتفق عليه بتاريخ 2026-06-06 — البند: [اذكره]. عايز تأكّد إنك عايز تغيّره؟»
   ولا تنفّذ إلا بعد تأكيد صريح، ثم **حدّث هذا الملف** بالقرار الجديد وتاريخه.
5. الأرقام في تقارير المدربين تُستخدم لحساب **المرتبات** → لازم تكون دقيقة 100%.
6. النشر: `git push origin main` يعمل deploy تلقائي (Railway للباك-إند، Vercel للفرونت). اعمل commit/push فقط لمّا الـ Owner يطلب، وبعد التحقّق.

---

## 1) الوصول للبيانات الحية + التحقّق (إلزامي)

**API قراءة-فقط مخصّص (gated):** `https://operation-system-production.up.railway.app/api/data-export`
- المصادقة: هيدر `X-Export-Key: <DATA_EXPORT_API_KEY>`. **المفتاح في ذاكرتك (auto-memory) أو اطلبه من الـ Owner** — لا يُكتب في الريبو.
- `GET /db` → **يحمّل snapshot كامل ومتسق للبيانات الحية** (كل الجداول، `VACUUM INTO`). ده الأهم.
- `GET /trainers` (team_members + extra_shifts + holidays) · `GET /lectures?from=&to=`
- `GET /remarks?q=` · `/absent?q=` · `/batches?q=` · `/users?q=` (بحث تشخيصي بالاسم/الهاتف/المجموعة).
- `GET /disk-usage` (2026-06-20) → تشخيص قراءة-فقط لمساحة الـ volume الحيّ: حجم `academy.db` الفعلي + WAL/SHM + مجلد الأڤاتار + `freelist` (المساحة القابلة للاسترجاع بـ VACUUM) + `statfs` (إجمالي/فاضي/% للـ volume) + عدّ جداول `*_backup`/`*_history`. لتقرير «VACUUM يستاهل؟ vs تكبير الـ volume». **قياس 2026-06-20: DB حيّ 375 ميجا (مضغوط 331 ⇒ VACUUM يرجّع ~44)، الأڤاتار 0.3 ميجا (مش السبب)، الـ volume 454 ميجا/84%. الخلاصة: التكبير هو الحل (DB بيكبر بالسجلّات)، والـ VACUUM محجوب لحد التكبير (محتاج مساحة مؤقتة ≈ حجم الـ DB).**

**تشغيل تقرير حقيقي على البيانات الحية (الطريقة الذهبية):**
```bash
cd academy-system/backend
KEY=<DATA_EXPORT_API_KEY>
curl -s -H "X-Export-Key: $KEY" -o _live.db "https://operation-system-production.up.railway.app/api/data-export/db"
# ثم استبدل auth middleware عبر require-cache وشغّل الـ endpoint الحقيقي على نسخة من _live.db:
cp _live.db _run.db
node -e "
  process.env.DB_PATH=require('path').join(process.cwd(),'_run.db');
  const ap=require.resolve('./src/middleware/auth');
  require.cache[ap]={id:ap,filename:ap,loaded:true,exports:{authenticate:(q,_,n)=>{q.user={id:1,role:'admin',line:'All'};n();},requireRole:()=>(_q,_s,n)=>n()}};
  const rp=require.resolve('./src/middleware/roles');
  require.cache[rp]={id:rp,filename:rp,loaded:true,exports:{requireRole:()=>(_q,_s,n)=>n(),requireAdminWrite:(_q,_s,n)=>n()}};
  require('./src/config/database').initDb();
  const app=require('express')(); app.use('/api',require('./src/routes/reports.routes')); app.use('/api/team',require('./src/routes/team.routes'));
  app.listen(0,async function(){const p=this.address().port; const j=await (await fetch('http://127.0.0.1:'+p+'/api/<route>?<params>')).json(); console.log(JSON.stringify(j).slice(0,4000)); process.exit(0);});
" ; rm -f _run.db _live.db
```
- قراءة مباشرة (للعدّ/التحقّق السريع): `node -e "const db=require('better-sqlite3')('_live.db',{readonly:true}); ..."`
- Node 24 + better-sqlite3 مثبّتين في `backend/`. أدمن line='All' → `lineFilter` يرجّع null (بلا فلتر لاين).
- **نظّف ملفات `_*.db*` المؤقتة بعد الانتهاء.** لا تعمل لها commit أبدًا.

---

## 2) نظرة عامة على النظام

- **الباك-إند:** Node/Express + **better-sqlite3** (disk-backed، القرص لا الذاكرة — حلّ مشكلة OOM 503). `backend/src/`.
- **الفرونت:** React (Vite) على Vercel. `frontend/src/pages/`.
- **مصدر البيانات:** ملفات Excel/تقارير من **Google Drive** + Center App (مالية). تتزامن عبر `sync.service.js` / `driveSync.service.js`.
- **خطوط (Lines / Tenants):** `Ahmed Hassan` و `Dardasha`. أدمن line='All' يشوف الكل.
- **ملف التقرير الرئيسي:** `backend/src/routes/reports.routes.js` (~6500 سطر) — معظم التقارير هنا.

### ملاحظة عن مجلدات Drive (مهم للتواريخ)
الفولدر في Drive بتاريخ **اليوم**، وجواه فولدرات «غياب زووم كول» و«غياب محاضرات أساسية» بتقارير **تاريخ اليوم الذي قبله بيوم** (فولدر 19 مايو = تقارير 18 مايو). النظام يخزّن **تاريخ المحتوى الصحيح** (وليس تاريخ الفولدر) — تم التحقّق: عدم تطابق غياب-الزووم سببه نقص بيانات وليس هذه الإزاحة.

---

## 3) القرارات وطرق الحساب المتفق عليها (baseline 2026-06-06) — أي مخالفة = تعارض

### إشغال المدربين والمرتبات (`/trainer-utilization`, `/trainer-utilization-summary`, `/find-available-trainer`)
- **المتاح (available)** = دقائق الشيفت (`end-start` − البريكات) عبر `[start_date,end_date] ∩ أيام العمل`، **+** ساعات `team_member_extra_shifts` لليوم. **الإجازات الرسمية** (`official_holidays`) مستبعدة. الفويس نوت **لا** يقلّل المتاح (وقت عمل لكنه ليس سعة).
- **المحجوز (booked) = الوقت الفعلي المشغول = اتحاد فترات المحاضرات + الفويس نوت (دمج المتداخلات `mergeIntervalsMinutes`)، وليس مجموع المدد.** الجلسات المتداخلة/المتزامنة (مدرّب زووم منسوب لكذا مجموعة في نفس اللحظة، أو نفس الجلسة بأكثر من لاحقة منسق) **تُحسب مرة واحدة**. الشغل المتتابع فوق الشيفت يظل >100% (مشروع). **(عُدِّل عمدًا عن قاعدة «عُدّ كل دقيقة بلا سقف» القديمة.)**
- **شيفتات متعددة:** `team_members.shifts_json` يحمل عددًا غير محدود من الشيفتات، لكل شيفت: مواعيد/أيام/قسم/بريكات/فويس نوت خاصة. `parseTeamShifts(t)` هو القارئ المعتمد (يصدّر `startMin/endMin` **و** `s/e` معًا — **لا تَحذف الـ s/e**). البريك/الفويس نوت قد يكون له `days` (يُطبَّق على تلك الأيام فقط).
- **بداية العدّ:** من `max(تاريخ التعيين, أقدم بداية شيفت)` (`trainerCountStart`) — منعًا لتضخّم النسبة قبل وجود شيفت.
- **نهاية العدّ + صفوف الشبح (تحديث 2026-06-08):** `trainerCountEnd(t,shifts)` = آخر `end_date` عبر كل الشيفتات، أو `null` لو أي شيفت مفتوح (المدرّب لسه شغّال). بعد هذا التاريخ تُستبعد **فقط** المحاضرات `مجدولة` (scheduled) من الـ booked في `/trainer-utilization` + `/trainer-utilization-summary` — لأنها **rows شبح**: لمّا المدرّب يسيب الشغل تُزال سلوتاته المتكررة من الشيت الحيّ، لكن الصفوف المستوردة سابقًا تبقى في الـ DB (الـ importer يحذف فقط مفاتيح group+date الموجودة في الملف الجديد) وتضخّم النسبة (Nashwa 265%، واتأكد من Drive أن آخر محاضرة لها 14/5 و الـ133 صف غير موجودة بالملف). **المحاضرات `مؤكدة` لا تُستبعد أبدًا** (مدفوع عليها فعلًا)، فآخر محاضرة حقيقية بعد الشيفت بيوم تظل محسوبة، ولا يتأثر أي مدرّب نشط. عدد المستبعَد يظهر كعلامة `stale_after_shift_end` (شارة «⚠ صف شبح» بجوار الاسم) لتنبيه الـ Owner لتنظيف الشيت.
- **ابحث عن مدرب:** كشف التعارض يقرأ من `lectures` مباشرة (**بلا INNER JOIN batches**) وإلا تُخفى تعارضات المجموعات المنتهية/المعاد تسميتها ويظهر المدرب «متاح» وهو مشغول.
- **السجلات المتعددة بنفس الاسم في فحص الجدول (2026-06-09):** `/code-problems` (فحص «محاضرة خارج وقت عمل المدرب») كان بيعمل `teamMap[الاسم] = t` فلو المدرّب له **أكتر من سجل** (متقسّم semi + private + تيم ليدر) **آخر سجل بس** (الأعلى id) هو اللي بيُستخدم. سجل التيم ليدر (أيامه أحد/أربعاء) كان بيتحقّق من محاضرات المدرّب الحقيقي (اثنين/خميس) → فلاجات وهمية. الحل: `teamMap` (1) **يستبعد التيم ليدر**، و(2) **يدمج `shifts_json`** لكل السجلات بنفس الاسم (المحاضرة تعدّي لو طابقت أي شيفت). (إصلاح Hassan Yasser: 3 سجلات.)
- **دمج سجلات المدرّب المتعددة في الإشغال (2026-06-09):** المدرّب المتقسّم لأكتر من سجل بنفس الاسم (مثلاً سجل semi قديم + سجل private جديد) كان بيتعدّ **مرتين** في `/trainer-utilization` — المحاضرات منسوبة بالاسم، فالسجل القديم (قسمه انتهى) كان لسه بياخد محاضرات المدرّب الحالية عبر fallback القسم → صف وهمي (متاح=0، محجوز>0) بيضخّم القسم. الحل: دمج السجلات بنفس الاسم (التيم ليدر مستبعد أصلًا بالـ query) في مدرّب واحد بكل شيفتاته، فمنطق `daySection` يوزّع كل محاضرة على قسم الشيفت **الشغّال** فعليًا في اليوم. **المهم:** السجل المدموج ياخد **أقدم تاريخ تعيين** (سجل اتعمل متأخّر لشيفت جديد بتاريخ تعيين حديث = 06-10 كان بيقصّ تاريخ بداية الحساب ويخفي الشهور الأقدم). (إصلاح Hassan Yasser: صف خاص واحد في يونيو بدل خاص+شبه‑خاص وهمي.)
- **القسم per-shift لا العمود الثابت (2026-06-09):** مصدر القسم المعتمد = `shifts_json[].section` (لكل شيفت بتاريخه)، **مش** عمود `team_members.section` الثابت (بيتأخّر لما المدرّب ينقل قسم). (1) **`/trainer-utilization`** في عرض «الكل» بيقسّم المدرّب **صف لكل قسم** عنده شيفت شغّال في الفترة (كل صف يحسب إشغال/متاح شيفتات القسم ده بس، date-aware عبر `buildRow(t,sec)`). (2) **`/find-available-trainer`** بيفلتر بالـ per-shift section **الشغّال في نافذة البحث** (`shiftActiveInWindow`) مش بالعمود الثابت، ويسمّي النتيجة بالقسم المطلوب. (3) **`/trainer-utilization-summary`** أصلًا per-section (`totalsBySectionForRange` + `row_key=id-sec`). الفرونت: مفتاح صف `id-section` + dedup تنبيه الـVoice-Notes بالـid. (مثال: Hassan Yasser اتنقل semi→private في 2026-06-01؛ يونيو=خاص، مايو+يونيو=صفّين.)
- **(2026-06-06) نافذة الوقت اختيارية في `/find-available-trainer`:** لو `from_time`/`to_time` فاضيين الاتنين → **وضع «كل الأوقات المتاحة»**: لكل يوم مختار يُحسب اتحاد الفجوات الفاضية = الشيفت − (الراحات + الـ voice notes + المحاضرات المحجوزة) عبر `freeIntervals`، والمدرّب «متاح» لو عنده أي فجوة ≥ 15 دقيقة (`MIN_GAP`)، وتُرجَّع الفجوات في `slot.free_slots`. لو الاتنين محدّدين → السلوك الأصلي (نافذة محددة). وقت واحد بس بدون التاني = خطأ. (الفرونت: خانة اختيار صريحة «ابحث بدون تحديد وقت» تعطّل حقلي الوقت وترسل وقت فاضي — لأن إفراغ `input type=time` يدويًا صعب؛ + عرض الفجوات في النتيجة.)
- **(2026-06-06) أنماط متكرّرة بأزواج الأيام في وضع «بدون وقت»:** الأكاديمية تجدول مجموعات على أزواج يوم: **السبت+الثلاثاء / الأحد+الأربعاء / الإثنين+الخميس** (`DAY_PAIRS`). لكل مدرّب يُحسب `day_patterns` (تقاطع الفجوات الفاضية عبر كل تكرارات اليوم في كل الأسابيع المختارة) و`pair_patterns` (تقاطع يومَي الزوج). القاعدة (قرار الـ Owner): السلوت «قابل للجدولة» فقط لو المدرّب فاضي على نفس الميعاد في **كل** تكرارات يومَي الزوج عبر **كل** الأسابيع المتتالية (تقاطع صارم via `intersectRanges`، ≥ `MIN_GAP`). الفرونت يعرض جدول الأنماط فوق + التفاصيل اليومية (قابلة للطي) تحته. (`free_ranges` رقمية داخلية تُحسب الأنماط منها ثم تُجرَّد من الـ payload.)
  - **تثبيت معنى العرض (مؤكَّد مع الـ Owner 2026-06-06):** جدول الأنماط (يظهر بالأصفر في الـ UI) = **القاسم المشترك الثابت** للساعات الفاضية عبر **كل** الأسابيع على يومَي الزوج = ساعات مضمونة للجدولة المتكررة. التفاصيل اليومية (تظهر بالأزرق) = **كل** الفجوات الفاضية لكل تاريخ على حدة — تشمل ساعات النمط **+** ساعات متغيّرة زيادة. أي أن الأنماط هي تقاطع مُستخرَج من التفاصيل، وليست «الساعات خارج النمط».
- **مصدر اسم المدرّب (مهم):** كل التقارير (أساسي + جانبي) تنسب المحاضرة للمدرّب من **عمود «المدرّب» المخصّص في شيت المحاضرات = العمود E / `r[4]`** (`excel.service.js:324` للأساسي و `:391` للجانبي) — **وليس** من الاسم المكتوب داخل أقواس اسم المجموعة. ترتيب الأعمدة: A=group_name, B=date, C=time, D=duration, **E=trainer**, F=status, G=location, H=attendance. المطابقة بفريق العمل = `stripParens(lectures.trainer)` (يشيل لاحقة `(Semi)`) ↔ `stripParens(team_members.name)`. ⇒ لإعادة نسب محاضرات مدرّب (مثل مدرّب ترك العمل) يجب تغيير **العمود E** في الشيت، لا اسم المجموعة. والاسم داخل أقواس المجموعة يُستخدم فقط لتحديد المنسق (اللاحقة بعد الأقواس).
- **حالة `غير مؤكدة`:** المحجوز يَعُدّ المحاضرات حتى لو `status='غير مؤكدة'` (لا فلتر حالة عام). أما `مجدولة` بعد مغادرة المدرّب فتُستبعد (انظر «نهاية العدّ + صفوف الشبح» أعلاه).
- **«خارج مهام عمله» (ساعات مدفوعة، 2026-06-08):** عمود في صفحة المرتبات بس (`showSalaryCategory`). ساعات بتاريخ تتدخل من مودال (تاريخ + من/إلى أو عدد ساعات + ملاحظة)، تُدفع بسعر الساعة المعتاد (`total_amount÷240`) وتُضاف للصافي. **قرار Owner قاطع: التواريخ دي لا تُحتسب في التشغيل (utilization) نهائيًا** — لذلك مخزّنة في جدول مستقل `team_member_outofduty_hours` منفصل تمامًا عن `team_member_extra_shifts` (اللي بيأثر على المتاح)، ولا يقرؤه أي حساب تشغيل. **(تحديث 2026-06-09: بقت لكل شيفت)** كل إدخال بيتنسب للشيفت اللي تاريخه واقع في مداه `[start,end]∩الشهر` ويُدفع بسعر ساعة **الشيفت ده**؛ المودال محدود بتواريخ الشيفت. `/api/team/outofduty-hours?from&to` بيرجّع الإدخالات الخام (مش إجماليات) عشان الفرونت يوزّعها. Endpoints: `/api/team/outofduty-hours?from&to` + `/:id/outofduty-hours` (GET/POST) + `/outofduty-hours/:entryId` (DELETE).
- **تقسيمة KPIs + اختيارها + قفل الشهر (2026-06-08):**
  - **تقسيمة (الجزء 1):** عمود `kpis` في `trainer_salary_defs` اتقسم لـ3: `kpi_no_absence` (عدم الغياب) / `kpi_on_time` (المواعيد) / `kpi_attendance` (حضور الطلاب) — قابلة للتعديل في صفحة تعريف الأنظمة. `kpis` (الإجمالي) = مجموع الـ3 دايمًا (يتحسب backend). الـ backfill قسّم القديم 50/25/25 (النِّسَب مش ثابتة — Owner بيحط المبالغ يدوي زي Part Time 300/100/100).
  - **اختيار (الجزء 2):** عمود "KPIs" في المرتبات → مودال بـ3 checkboxes (بمبالغ نظام المدرّب)؛ المختار يتضاف للصافي. **اختيار يدوي**. **(تحديث 2026-06-09: لكل شيفت)** مخزّن لكل (مدرّب+شهر+`shift_index`) في `trainer_kpi_awards` (upsert) — كل شيفت ينظامه ومبالغه. تناسب الـKPI = نفس نسبة تناسب أساسي **نفس الشيفت** (`base÷baseFull`)، فبيتقسم زي الأساسي تلقائيًا. `net = base + extraPay − ded + outOfDuty + kpi + adminBonus − adminDed`.
  - **مكافأة/خصم إداري (2026-06-09):** عمودين في المرتبات لكل شيفت — مبالغ ثابتة `trainer_admin_adjustments(trainer_id,month,shift_index,bonus,deduction)`. `net += bonus − deduction`. Endpoints `/trainer-salaries/admin-adjustments` (GET?month / PUT upsert). محفوظة في snapshot القفل. + تحسين بصري للجدول العريض: تثبيت عمود اسم المدرب (sticky) + تلوين صفوف بالتبادل.
  - **قفل الشهر (الجزء 3) — الحماية:** صفحة المرتبات بتحسب **لحظيًا** من القيم الحالية (مفيش effective-dating) ⇒ أي تغيير في الأنظمة/KPIs بيأثر **بأثر رجعي** على الشهور السابقة. الحل: زرار "قفل الشهر" بيصوّر صفوف المرتب المحسوبة كاملة في `payroll_month_locks` (JSON). الشهر المقفول بيتعرض من الـ snapshot (مش إعادة حساب) وكل التعديلات بتتقفل ⇒ محصّن. الفتح بيمسح الـ snapshot. Endpoints: `/trainer-salaries/locks` + `/locks/:month` (GET/POST/DELETE). **القاعدة:** اقفل الشهر قبل ما تغيّر أي أسعار عشان متتأثرش.
- **«تيم ليدر» = إداري لا مدرب (2026-06-08):** موظف الإدارة التعليمية بـ `job_title='تيم ليدر'` مسؤول إداري فقط → **يُستبعد** من كل تقارير الإشغال/المدربين (`/trainer-utilization`, `/trainer-utilization-summary`, `/find-available-trainer`, و`/trainer-work-history` = سجل عمل المدربين) عبر `AND (job_title IS NULL OR job_title <> 'تيم ليدر')`. **لكن يظهر في «مرتبات المدربين»** لأنه بياخد مرتب إداري — صفحة المرتبات بتبعت `include_leaders=1` لـ `/trainer-work-history` فيرجع التيم ليدر. (القيم الأخرى منسق/مدرب/null ما تتأثرش.) **(تحديث 2026-06-09):** صف التيم ليدر **لا تُنسب له أي ساعات تدريس** — `extra_minutes`/`unconfirmed_minutes` = صفر إجباري (لأن نسب الساعات بالاسم، ولو الشخص مدرّب وتيم ليدر بنفس الاسم كانت الساعات تتكرر على الصفّين). صف المدرّب يحتفظ بساعاته. (إصلاح Heba(M.P) + Heba(M.P)(T.L).)
- **تناسب الأساسي للشهر الناقص (2026-06-08، قرار Owner = أيام تقويمية):** لو الشيفت غطّى جزء من الشهر، الأساسي = `(total÷30) × عدد الأيام التقويمية اللي الشيفت [start,end] غطّاها في الشهر` (inclusive، عبر `prorateBase`). تغطية الشهر كامل → الأساسي كامل (مع cap، مفيش زيادة في شهور 31 يوم). على variant الـ `monthPicker` بس. الخلية بتعرض `proDays/monthDays يوم`. الساعات الإضافية/الخصم/KPI/خارج المهام مش متأثرين. (إصلاح: Esraa Hani كانت بتاخد مرتبين كاملين 8000+6000 لشهر ناقص.)

### حالة المحاضرة (Arabic STATUS) + عدّ المحاضرات
- قيم `lectures.status`: **`مؤكدة`** (اتعملت فعلًا/delivered)، **`مجدولة`** (محجوزة/scheduled، لسه ماتعملتش)، **`غير مؤكدة`** (unconfirmed)، **`ملغية`** (cancelled).
- **عمود اسم المجموعة قد يحوي ملاحظات مش مجموعات حقيقية** — تُستبعد من حساب «ساعات غير مؤكدة» (المرتبات) + نافذتها: `Voice Note` / `Break` / `Test` / **`تأجيل` (تأجيل برسوم = محاضرة مؤجَّلة لعدم الدفع، تحديث 2026-06-09)** عبر `group_name NOT LIKE '%...%'` في `/trainer-work-history` + `/trainer-work-history/unconfirmed`.
- **سجل تغييرات الجدول (`schedule_change_log`) — كشف التلاعب (Phase 1، 2026-06-09):** **مشكلة العمل:** موظفون يحذفون محاضرات `غير مؤكدة` من الشيت (لإخفاء غياب مدرب) أو يضيفون محاضرة مجانية ويحذفون أخرى لتثبيت العدد. **الحل:** أُعيد تفعيل **الكشف الحيّ** (كان معطّلًا عمدًا — القرار اتغيّر بموافقة Owner) بشكل موسّع: كل مزامنة محاضرات/جانبي، `recordScheduleChanges` بيعمل diff بين الحالة القديمة في DB (مُلتقطة قبل الـ delete-replace، في نطاق مجموعات+تواريخ الملف) والملف الجديد، ويسجّل كل **added/deleted/moved** بتوقيته في جدول append-only `schedule_change_log`. **الأهم: بيسجّل الحذف الصريح** (اللي كان متجاهَل). المزاوجة كـ«نقل» **محافِظة**: تتجمّع لـ`moved` بس لو group+trainer له حذف واحد وإضافة واحدة (نقل واضح)؛ غير كده تُسجَّل منفصلة (مايخفيش حذف). الجدول بيتملي **من وقت النشر للأمام** (مفيش تاريخ قديم للحذف — كان ضايع). **dedup:** الصف المحذوف بيفضل في DB (حذف جزئي) فكل مزامنة تعيد كشفه؛ `recordScheduleChanges` بيتخطّى أي تغيير متسجّل (نفس type+group+line+session+date+time+new_date). **Phase 2 (2026-06-09):** `/backfill-from-drive` (زر «فحص ذكي») بقى يستدعي `recordScheduleChanges` على كل لقطتين Drive فيسجّل الحذف/الإضافة في الـ ledger (مش بس النقل في `lecture_reschedules`) — فيكشف الحذف في الماضي القريب من أرشيف Drive. **Phase 3 (2026-06-13) — علامات اشتباه:** `flagFor(type, prevStatus)` بيحسب `flags` وقت الإدراج: حذف+`مؤكدة`→`deleted_confirmed` (محاضرة مُسلّمة اختفت = اشتباه عالٍ)، حذف+`غير مؤكدة`→`deleted_unconfirmed` (غياب مخفي محتمل)، حذف آخر→`deleted_scheduled`، إضافة→`added_extra`، نقل→`null`. **Phase 4 (2026-06-13) — واجهة المراجعة:** 3 endpoints في `reschedules.routes.js` (كلها `requireSuperAdmin`): `GET /reschedules/change-log/summary` (عدّادات by_type/by_flag/by_review)، `GET /reschedules/change-log` (فلاتر type/flag/review/group/trainer/line/from/to + limit/offset)، `PATCH /reschedules/change-log/:id/review` (`new`|`reviewed_ok`|`violation`). الصفحة `ScheduleChangeLog.jsx` على `/admin/schedule-changes` («كشف تلاعب الجدول» في السايدبار، Quality) — كروت ملخّص + فلاتر + جدول بشارات نوع/اشتباه ملوّنة + أزرار مراجعة (سليم/مخالفة). كل المراحل متحقَّقة على لقطة حية واتنشرت (22ab1eb).
- **عدّ المحاضرات المُسلّمة (KPIs الداشبورد: main_lectures/side_sessions/zoom_calls) = `status='مؤكدة'` فقط** (تحديث 2026-06-08). كان الكود يفلتر `!= 'غير مؤكدة'` فيشمل `مجدولة` → الرقم متضخّم ~2× (نصّه محاضرات مجدولة مش مُسلّمة، ومنها صفوف شبح مش في الشيت). القرار: المُسلّم = `مؤكدة`. (الدمج: `COUNT(DISTINCT <canonical session>)` لمنع توائم إعادة التسمية.)
- **استبعاد المجموعات الداخلية من عدّادات الداشبورد (تحديث 2026-06-08):** `notInternalGroup` مطبَّق على **عدّ المحاضرات** (main/side/zoom — شال 266 جلسة placement/free-slot) **وعدّادات المجموعات** (نشطة 689→685، waiting 34→32 و20→17، expired) — زي باقي التقارير. الداخلي = `free slot` / `hiring new teacher` / `placem…test` / `تحديد مستوى`.
- **كارت «مجموعات مكررة» (`duplicate_groups` + `duplicate_groups_list`) (تحديث 2026-06-08):** يعرض المجموعات اللي ليها أكتر من صف في `batches` لنفس `(group_name,line)` (KPI = عدد المجموعات المميّزة، القائمة = كل صفوفها بحالاتها للتنظيف). يستبعد الداخلية + **استبعاد خاص بالكارت فقط: `Go English` و`Grammar`** (`AND LOWER(group_name) NOT LIKE '%go english%' AND ... NOT LIKE '%gramm%'`). **مهم:** مجموعات Grammar **حقيقية** (لها 68 محاضرة مؤكدة) فمُستبعَدة من **الكارت فقط** — تظل محسوبة في كل تقارير الأداء (غياب/متوقع/جودة). Go English (0 محاضرات) غير مؤثرة أصلًا.
- **البيانات تتراكم + الحل الجذري (تحديث 2026-06-08):** الـ importer يحذف فقط مفاتيح group+date الموجودة في الملف، فكانت تتراكم صفوف قديمة (~50% من lectures مش في الشيت). نوعان: (أ) `مؤكدة` قديمة = **تاريخ حقيقي محفوظ عمدًا** (مجموعات منتهية/معاد تسميتها) — **تفضل وتُعدّ**؛ (ب) `مجدولة` قديمة = **شبح**. **الحل الجذري:** `syncLectures`/`syncSideSessions` دلوقتي بعد كل استيراد بينادوا `archivePhantomScheduled(line,type,fileRows)` اللي بينقل أي صف **`مجدولة`** مش في الملف الكامل إلى **`lectures_history`** (أرشفة قابلة للتراجع، **لا تلمس `مؤكدة` أبدًا**). محميّ بحارس `detectAnomaly` (يرفض الملفات الناقصة) + مطابقة group بدون مسافات/حالة. التنظيف يحدث في أول مزامنة بعد النشر (تستخدم أحدث ملف). ⇒ الشبح لا يتراكم. مع ذلك تظل القاعدة العامة: أي عدّ من `lectures` = `مؤكدة` فقط (للمُسلّم) **أو** `COUNT(DISTINCT canonical)` rename-aware.
- **مكالمات الزوم تتسرّب لـ main → حارس + تنظيف (2026-06-20):** `session_type='main'` بييجي **فقط** من ملف فولدر «Lecture Main Session» (`parseLectures` بيحطّه ثابت بلا تصنيف). ملف Drive غلط/مدمج (`Lectures (1).xlsx`, 22,197 صف ≈ ضعف الطبيعي، اتحطّ trash بعدين) اتزامن 2026-06-20 07:49 فدخّل **مكالمات زوم (مدة 00:15، مدرب بلاحقة `(Z.C)`)** كـ main → تقرير «غياب المحاضرات الأساسية» Part2 (محاضرة مؤكدة بلا حضور → كل المجموعة غايبة) ولّد **غياب وهمي** لعملاء بلا سجل غياب حقيقي. **مؤكَّد بسجل `excel_syncs`** (الملف ده اتزامن main+side). متحقَّق: 15,439 صف main بصمتهم زوم، و**0** منهم محاضرة حقيقية (المحاضرات 01:30 ومفيش فيها `(Z.C)` ولا واحدة). **الحل (commit f7bd6bc):** (1) **حارس** في `parseLectures` يتخطّى صفوف `isZoomCallTrainer` (لاحقة `(Z.C)`) فمايدخلوش main أبدًا (يتسجّلوا side من ملفهم الصح زي ما هم)؛ (2) **migration تنظيف لمرة واحدة** في app.js يأخذ نسخة احتياطية (`lectures_zoom_misclassified_backup`) ثم يحذف صفوف الزوم-كـ-main (idempotent، قابل للتراجع). متحقَّق حيّ بعد النشر: 15,439→0، النسخة الاحتياطية 15,439، المحاضرات الحقيقية 2,100 ثابتة، والمجموعة المثال 06-18 من 9 صفوف وهمية → 0. **مهم:** إعادة رفع الملف الصح **لا تمسح** الصفوف الوهمية القديمة (الـ importer يحذف فقط مفاتيح group+date في الملف الجديد؛ تواريخ الزوم الوهمية مش في الملف الصح) — لازم تنظيف صريح.
- **أداة «حذف بيانات استيراد يوم معيّن» (2026-06-20، `drive.routes.js`):** للـ Owner لمّا يرفع شيتات غلط في يوم. 3 endpoints أدمن على `/api/drive`: `GET /import-by-date?date=` (معاينة عدد الصفوف لكل جدول عبر `date(synced_at)=date`)، `POST /import-by-date/delete` (نسخة احتياطية JSON في `import_rollback_backup` ثم حذف)، `POST /import-by-date/restore` (إعادة إدراج من النسخة = تراجع كامل). على الجداول السبعة المستورَدة (lectures/absent_students/absent_zoom_students/batches/clients/employees/remarks). آمن لأن **Smart-Sync يتخطّى الملفات غير المتغيّرة** فـ `synced_at` ليوم = رفع ذلك اليوم فقط. الواجهة: قسم في صفحة DriveSync (تاريخ + معاينة + حذف + استرجاع). متحقَّق على نسخة: round-trip حذف→استرجاع يرجّع الحالة بالظبط (747 صف ليوم 2026-06-18). **حدّ:** الحذف يشيل صفوف اليوم؛ لاسترجاع بيانات قديمة دهسها الرفع الغلط ارفع الشيت الصح. [نُشر — commit a3281b3.]
- **أداة «أرشفة الجداول الباردة للدرايف» (2026-06-20، `coldArchive.service.js` + `cold-archive.routes.js`):** الهدف تقليل مساحة الـ Railway volume (كان 84-92%). فحص الكود أثبت إن جداول معيّنة **مفيش أي route/service بيقرأها** (كتابة-فقط): `lectures_history` (أرشيف الشبح، `INSERT` بس في sync.service:848)، `absent_students_history` + `absent_zoom_students_history` (`dedupeAbsenceTable` بيكتب فيها بس)، و`group_renames_backup_20260606`. **whitelist صارم** في الخدمة يرفض أي جدول تاني (مثلاً `remark_assignment_history` **دافئ** — يُقرأ في reports.routes:506؛ `remark_snapshot_rows` دافئ — Remarks Monitor؛ `import_rollback_backup` وظيفي). الأداة بتشحنها للدرايف (`System DB Archive/<table>/*.jsonl.gz`) وتحذفها من الـ DB. **الأمان:** الرفع يُعاد تنزيله ويتحقّق `sha256`+عدد الصفوف **قبل أي حذف**؛ الحذف يستهدف rowids مُلتقَطة **قبل** رحلة الدرايف (concurrency-safe مع أي sync متزامن)؛ عكسية كاملة عبر `/restore`؛ سجل تدقيق `cold_archive_log`. `VACUUM` منفصل/يدوي/محمي بفحص مساحة حرّة (better-sqlite3 متزامن → يجمّد السيرفر، عمره ما يشتغل أوتو). Endpoints (`requireSuperAdmin`): `/api/cold-archive/{preview,run,log,restore,vacuum}`. cutoff افتراضي = أقدم من 30 يوم (NULL timestamp = مؤهّل). الواجهة قسم في DriveSync. مكسب مقيس بالمحاكاة على snapshot حيّ: حذف الباردة + VACUUM = 331→284 ميجا مضغوط (~47 ميجا)، وعلى الملف الحيّ 375→~284 (~91 مع التفتيت). دورة أرشفة→حذف→استرجاع متحقَّقة lossless (نص عربي+NULLs، drive موهوم). **النشر لا يحرّك بيانات** — الأرشفة تشتغل بضغط زر فقط. ملاحظة: الرفع/التنزيل الفعلي للدرايف لم يُختبر على درايف حيّ (mock فقط) لكن «تحقق-قبل-حذف» يؤمّنه بالتصميم. [نُشر — commit e46572c. ملف الدرايف بصلاحية read-write أصلاً، بلا credentials جديدة.] انظر [[project_volume_capacity]].
- **نفس المرض في `absent_students` + `absent_zoom_students` (تحديث 2026-06-08):** لما المنسق يصحّح مجموعة سجل غياب ويعيد رفع الملف، الـ importer يحذف فقط مفاتيح (group+date) في الملف الجديد → الصف القديم بالمجموعة الغلط **يفضل شبح** ويضخّم غياب تلك المجموعة/المنسق (مثال: «دكتور ابتسام» اتسجّلت غياب تحت Con2 وهي مش عضو؛ Nourhan Ragab غيابها كان متضخّم 397→245). كان عندنا **749 صف أساسي + 1108 زوم شبح**. **الحل:** `dedupeAbsenceTable(tbl,histTbl,line)` (sync.service) — تنادى في `syncAbsent`/`syncAbsentZoom` بعد الإدخال — تُبقي **الأحدث `synced_at`** لكل `(line, phone-or-name, date, time)` وتنقل الأقدم لـ `absent_*_history` (قابل للتراجع). آمن: الطالب لا يكون غايبًا من مجموعتين في نفس اللحظة، فنفس المفتاح = نفس الحدث (صف واحد ينجو). تُطبَّق على manual فقط (`auto_generated=0`)؛ صفوف auto تُعاد توليدها. التنظيف يحدث في أول مزامنة غياب لكل لاين. التحقّق: الجودة=الحضور والمقاطع=الصف لسه مطابقين بعد التنظيف.

### فحص الأكواد (`/code-problems`) — التحقق من الجدول صار نافذة-واعٍ + DISTINCT (تحديث 2026-06-17)
- **المشكلة:** `computeCodeProblems` كان بيتحقق من جدول المجموعة على **صفوف الـ lectures الخام المتراكمة** (يعدّ الصفوف لا التواريخ المميزة، بلا حصر بفترة، INNER JOIN batches بلا dedup)، فأنتج false positives كتير: «عدد محاضرات زيادة» متضخّم بالتوائم/الصفوف القديمة، و«أول/آخر محاضرة غلط» متسلسل من صف غريب أقدم. مثال مُتحقَّق حيّ: `Jun_14_Sun_9pm_General_2_SP(Israa Hafiz)Roaa` الشيت فيه 8 محاضرات بس التقرير قال «زيادة 10» + أول تاريخ 06-07 (صف «Amira semi cs» غريب مؤكد اتسجّل غلط تحت الكود) + آخر تاريخ متسلسل غلط. السبب الجذري: الـ importer يحذف مفاتيح group+date الموجودة في الملف فقط، و`archivePhantomScheduled` يأرشف `مجدولة` فقط (يسيب `مؤكدة` عمدًا) ⇒ صفوف مؤكدة قديمة/غريبة تعيش للأبد.
- **الحل (logic-only، بلا لمس بيانات — متوافق مع قاعدة «أي عدّ = COUNT(DISTINCT)»):**
  1. **نافذة الباتش:** التحقق يقتصر على المحاضرات داخل `[batch.start_date, batch.end_date]` (حدّ مفتوح لو التاريخ ناقص). الصفوف القديمة/الغريبة برّه النافذة تُستبعد من العدّ/التواريخ.
  2. **العدّ بـ DISTINCT DATE** لا صفوف خام (يطوي توائم RENAME/إعادة الاستيراد اللي بتشترك في التاريخ وتختلف في الوقت/المدرب = يوم تدريس واحد).
  3. **`SELECT DISTINCT` في mainRaw/sideRaw + dedup مصفوفة الباتشات** بـ `(group_name|line)` ⇒ المجموعة بصفّين نشطين (مثل Sara Ezat Con_3) ما تتعدّش/تتفلجش مرتين (كان 58=29×2).
  4. **استبعاد buckets غير حقيقية:** أضيف `placement/تحديد مستوى/تعويض` للـ skip (مع free slot/hiring القديمين).
  5. **فحص جديد منفصل «محاضرة/زووم كول خارج فترة المجموعة»:** الصفوف برّه `[start,end]` تظهر بنوعها الحقيقي بدل ما تفسد العدّ/التواريخ — بيكشف الصفوف الغريبة/القديمة صراحةً.
  6. **«آخر محاضرة/زووم غلط» بالتاريخ الخام لا `effectiveDate`:** كان بيقارن `effectiveDate` (يزوّد يوم لو السيشن عدّى منتصف الليل) بـ `expectedMainLast` (تاريخ خام) ⇒ كل مجموعات الـ11PM تتفلج +1 وهمي رغم إن جدولها صح. الآن المقارنة بالتاريخ الخام (ملاحظة «تعدى منتصف الليل» معلومة هامشية بس). شال 55 false positive.
- **متحقَّق على snapshot حيّ:** مجموعة Israa Hafiz بقت 8 (صح) + أول تاريخ 06-17 (صح) + الصفّين الغريب/القديم في «خارج فترة المجموعة». الإجمالي النشط 901→868 بعد إزالة الزائف، و**كل الأنواع المتبقية تأكدت حقيقية بإعادة اشتقاق مستقلة** (زيادة/ناقصة/أول-تاريخ/أيام/خارج-فترة/زووم-count). فلاجات «خارج وقت عمل المدرب» (Nada(Z.c)/Ayat Saleh انتهى عملهم 06-30 ولهم جلسات بعدها) **حقيقية ومؤكدة**.
- **الشيت الحالي فقط — الفحص يقرأ أحدث استيراد لكل مجموعة (قرار الـ Owner 2026-06-18):** `code-problems` كان بيقرأ **كل** صفوف `lectures` المتراكمة، فالمحاضرات المؤكدة القديمة اللي اتشالت من الشيت (الـ importer يحذف مفاتيح group+date في الملف فقط، و`archivePhantomScheduled` يسيب `مؤكدة` عمدًا) كانت بتتفلج كـ«خارج فترة المجموعة» رغم إنها مش في الشيت الحالي (مكانها «إعادة جدولة المحاضرات»). **الحل:** `mainRaw`/`sideRaw` بقوا يضمّوا `INNER JOIN (SELECT group_name,line,date(MAX(synced_at)) AS sync_day … GROUP BY group_name,line) ls` ويفلتروا `date(l.synced_at) = ls.sync_day` ⇒ صفوف **أحدث يوم سينك للمجموعة بس** = الشيت الحالي. الإشارة دقيقة: الملف اليومي يعيد إدراج صفوفه بـ `synced_at` جديد، والصفوف المشالة تحتفظ بـ `synced_at` أقدم (مثال مُتحقَّق: `Jun_14_sun_10Pm_General2_SP(Loaa Ali)roaa` محاضرة 10/06 synced 06-16 بينما الشيت الحالي synced 06-18 → اختفت من الفحص). متحقَّق حيّ: المجموعة دي صارت 0 مشاكل، وكل تواريخ «خارج فترة المجموعة» المتبقية في الشيت الحالي فعلًا (0 صف قديم). [نُشر — commit (code-problems).]
- **آخر محاضرة/زووم صارت holiday-aware (تحديث 2026-06-18):** `expectedMainLast`/`expectedSideLast` كان offset ثابت (+24/25 و+21) لا يحسب الإجازات، فأي مجموعة بدأت في مايو وامتدّت عبر **عيد الأضحى (2026-05-26→06-01، 7 أيام)** كان آخر تاريخها الحقيقي متأخّر 7 أيام → **31 false positive بفرق +7 بالظبط**. الحل: `_extendPastHolidays(first, baseOffset)` يضيف عدد أيام الإجازة (من `_holidayRanges()`) الواقعة داخل المدى للـ offset (مع تكرار ليشمل إجازة في الذيل الممتد). متحقَّق حيّ: «آخر محاضرة/زووم» 84→60 (شال 24 زائف عيد)، والـ7 المتبقية المتقاطعة مع العيد = انحرافات حقيقية بعد تصحيح الإزاحة. المجموعات اللي لا تعبر إجازة لا تتأثر (الإضافة=0).
- **«كود غير مطابق لمستوى المدرب» = حقيقي (تأكيد 2026-06-18):** Mariam omara وIKRAM الاتنين `teachable_conversation=0` (قيمة متسجّلة عمدًا جنب starter=3/general=5) وفعلًا مدرّبو مجموعات Con 1 (`P Con 1`/`Private conversation 1`) في عمود trainer. فالفلاج صحيح (إسناد مخالف فعلي، أو الـcaps محتاجة تحديث من الـ Owner) — **لا false positive**.

### حالة الريمارك (Arabic STATUS)
- القيم الفعلية فقط: **`إنتهت`** (منتهية/done) و **`غير منتهية`** (مفتوحة). عداد «الريماركات المفتوحة» = `غير منتهية` فقط (يجب استبعاد `إنتهت`). أي قائمة closed تشمل `إنتهت`. Remarks Monitor يعتبر `إنتهت` = resolved.

### المنسقون (NAME-KEY)
- مفتاح المطابقة = **اسم الفريق/الـ username** (مثل `Malika7`, `yassmen`, `RadwaGamal`) كما في `coordinator_history.coordinator` / `batches.coordinators` / `team_members.name` — **وليس** `users.full_name` («Malika Dardasha» ≠ «Malika7»).
- المطابقة تتم بمفتاح **مضغوط** (إزالة المسافات + lowercase): «RadwaGamal» = «Radwa Gamal» (`userDeptExpr`, `coordStrHasName`).
- قوائم/أعمدة المنسقين تعرض **فقط** المنسقين المسجّلين في **فريق العمل** (`team_members`). الأسماء غير المسجّلة و الـ placeholder **`--`** تُعرض **فارغة**.
- منسق فريق خدمة العملاء النشطين = `team_members WHERE department='customer_services' AND status='active'`. dropdown تقارير خدمة العملاء يأتي من هؤلاء (لا من `users`).
- **النسب المنتهية (المجموعة بمنسقين):** كل حدث يُنسب لـ **منسق واحد** = الأقدم (`ORDER BY effective_from ASC, coordinator ASC LIMIT 1`) — لا GROUP_CONCAT ولا حساب مزدوج.
- **`/attendance-absence/segments` لازم يطابق صف `/attendance-absence` بالظبط (تحديث 2026-06-08):** الـ drill-down انحرف عن الصف؛ يطابقه في 4 نقاط: (1) دالة `cf` تطابق `coordinator_history` عبر **`effectiveGroupNameAtDate`** (rename-aware) لا الاسم الخام — السبب الجذري لانحراف النسب (RadwaGamal 266→95)؛ (2) `DEDUP_BATCHES`؛ (3) `notInternalGroup` في كل استعلاماته؛ (4) غياب الزوم من الملف المرفوع `absent_zoom_students`. تحقّق: مجموع المقاطع (غياب + متوقع) = الصف لكل المنسقين (0 انحراف).
- **المتوقع (denominator) + تكرار batches (تحديث 2026-06-08):** «متوقع» الجلسات الأساسية = `SUM(MAX(عدد المتدربين, الحاضر+الغايب))` لكل محاضرة. مصدر عدد المتدربين = `batches.trainee_count` (المجموعات المنتهية «إنتهت» مستورَدة بالفعل في batches، 9020 صف، فتؤخذ منها مباشرة)، وإلا عدد العملaء، وإلا (للمجموعات غير الموجودة في الشيت) الحاضر+الغايب. لإضافة دقة لمجموعة منتهية: ضِفها في شيت Batches بحالة **«إنتهت»** + عدد متدربين (لا تُحسب نشطة لأن العدّادات تفلتر `status='نشطة'`). **مهم — تكرار batches:** أي JOIN على `batches` على `group_name` يُضاعف الصفوف لو المجموعة لها أكثر من صف (نشط+«إنتهت» أو تكرار placement — 32 مجموعة اليوم). الحل: helper موحَّد **`DEDUP_BATCHES`** (`SELECT group_name,line,MAX(coordinators),MAX(dept_type),MAX(trainee_count),MAX(lecture_duration_min) FROM batches GROUP BY group_name,line`) بدل `batches` المباشر. مطبَّق على: المتوقع (attendance-absence + quality)، `lectures-list`، `attendance-absence-by-department`، و absent_main في الداشبورد. تحقّق فرقي (as-is مقابل batches موحَّد) = صفر فرق في تلك التقارير. (عدّادات المجموعات في الداشبورد — نشطة/منتظرة/منتهية الصلاحية — تستبعد المجموعات الداخلية `notInternalGroup` مثل باقي التقارير: active_groups 689→685، waiting 34→32 و20→17، بعد استبعاد placement test / free slots. تكرار صف نشط حقيقي نادر (مثل مجموعة مكتوبة «بانتظار» + «نشطة» معًا) = تنظيف بيانات من جهة الـ Owner.)
- **غياب موظفي الجودة = غياب الحضور/الغياب (تحديث 2026-06-08):** `quality-employee` كان ينسب الغياب عبر `batches.coordinators` الحالية (نموذج مختلف + يُسقط المجموعات المنتهية) → أرقام لا تطابق `/attendance-absence` (مثال: shrouk gamal 0 مقابل 129، doha 123 مقابل 196). الآن يبني نفس خرائط الغياب per-coordinator بنفس منطق `dateAwareCoord` (coordinator_history + عضو روستر مُوظَّف + منسق واحد أقدم + ended-group resilient) ويبحث بالاسم المضغوط ⇒ الرقمان متطابقان لكل منسق. أي تعديل في حساب غياب أحد التقريرين يجب أن ينعكس في الآخر.

### المجموعات المعاد تسميتها (RENAME)
- عند إعادة التسمية: **`lectures`/`batches` بالاسم الجديد**، لكن **`absent_students`/`absent_zoom_students` بالاسم القديم**، و**`group_renames`** يخزّن (old→new). أي مطابقة غياب↔محاضرة/batch بالاسم بالضبط هشّة.
- **القاعدة:** كل مطابقة لازم تكون **rename-aware**: طابق بالاسمين عبر `currentGroupNameExpr(group,line)` (old→new) أو `effectiveGroupNameAtDate` (new→old، للأحداث قبل التسمية). مع تطبيع المسافات `REPLACE(...,' ','')`.
- **⚠ تلوّث `group_renames` بسجلات مكررة/معكوسة (تشخيص 2026-06-06):** الـ Drive sync **يعيد تسجيل** نفس التسمية في كل تشغيلة بختم `renamed_on` = تاريخ التشغيلة (تسمية واحدة حقيقية NORHAN→Radwa يوم 06-01 تظهر مكرَّرة 06-04/06-06)، **ويصدّر الحافة المعكوسة** أيضًا (Radwa→NORHAN وهمية). الجدول: 1888 صف / 794 مميَّز / 717 بأكثر من تاريخ / 324 زوج معكوس / 594 اسم canonical غير مستقر. هذا كسر الدالتين فطلعت أصفار غياب وهمية + تضاعف عدّ المحاضرات لكل منسق. **الإصلاح (logic-only، بلا لمس بيانات):** (1) `effectiveGroupNameAtDate` لا يرجّع الاسم القديم إلا إذا كان تاريخ الحدث **أقدم من `MIN(renamed_on)`** للاسم الجديد (يتجاهل التكرارات المتأخرة)؛ (2) `currentGroupNameExpr` **يرسو على `batches`**: الاسم الموجود حاليًا في `batches` هو الحالي (القديم يُحذف عند التسمية) → canonical مستقر وصحيح. غياب RadwaGamal: 0/0 → **18/19** (مطابق للمودالز). **بند معلّق (جذر):** إصلاح `driveSync` ليتوقف عن توليد السجلات المعكوسة/المكررة + تنظيف الجدول.
- **تكرار المحاضرات (~40%):** الـ importer يحذف `WHERE group_name=? AND date=?` فقط، فصفوف الاسم القديم تتراكم (~26k صف توأم). **القرار: لا تُحذف** (الحذف أثبت أنه يفقد ~11k جلسة فريدة). بدلًا منها: التقارير تتعامل معها — الإشغال يدمج الفترات، والعدّ يستخدم `COUNT(DISTINCT <canonical session>)`.
- **كشف الـ rename في «الفحص الذكي» (Drive backfill) — `detectRenamesFromSnapshots` (2026-06-13، commit 24bd57f):** الـ `syncBatches` الحيّ بيكشف التسميات، لكن `/backfill-from-drive` كان يكشف **إعادة الجدولة فقط**؛ فتغيير اسم المدرّب/المنسق لنفس السلوت داخل نافذة الفحص (مثال حقيقي: `...Con_5_P(Nourhan Samy)magdy → ...Con_5_P(Mohamed Abdulkhaleq)magdy`، أُكِّد من ملفي batches 10/6 و13/6) ما كان يوصل لـ `group_renames`، فالـ «قصة» ما تعرض الكود الجديد. الدالة (في `sync.service.js`، تُستدعى داخل الـ backfill على لقطتي الحدّ) بتقارن الاثنتين وتسجّل old→new بـ `detected_by='drive-backfill'`. **محافِظة — ليست دمج level+trainer الممنوع** ([[feedback_no_loose_group_linking]]): الـ stable id = البادئة كاملة قبل أول `(` (تاريخ+يوم+وقت+كورس عبر `getStableIdentifier`)، وتسجّل تسمية **فقط** لمّا يختفي اسم واحد ويظهر اسم واحد لنفس السلوت (فصلان متوازيان = اسمان → تُتجاهَل)، مع حِراسة العكس/التكرار (لا A↔B cycle). متحقَّق على لقطتين حقيقيتين (641 مقابل 636 مجموعة): مسك الزوج الصحيح ضمن 38 زوج 1↔1 وتخطّى المسجَّل سابقًا (بلا تكرار). **ملحوظة:** يمسك فقط التسميات اللي كودها الجديد يظهر فعلًا كـ `group_name` في ملفات Drive داخل النافذة؛ لو الشيت غيّر **عمود المدرّب فقط** والاسم ثابت فلا rename للكشف. (في الحالة المذكورة الـ auto-sync الحيّ سبق وسجّلها فور مزامنة batches 13/6 — كانت مسألة توقيت.)

### المجموعات المنتهية (ENDED → تختفي من `batches`)
- المجموعة لمّا تخلص **تُحذف من `batches`** لكن تبقى في `lectures` + `coordinator_history`. أي `INNER JOIN batches` يُسقطها (عدّ ناقص / قسم «—»).
- **القاعدة:** استخدم **`LEFT JOIN batches`** واسترجع المنسق/القسم من `coordinator_history` (`coordAtDateSingleExpr` → `users.department`/قسم الفريق) عند غياب الـ batch. للعدّ: `LEFT JOIN` + `COUNT(DISTINCT canonical)`.
- فلتر القسم في تقرير الحضور: إن لم يوجد سجل `team_member_dept_history` يغطّي التاريخ → fallback إلى `team_members.section` (وإلا يظهر المنسق بأصفار).

### المجموعات الداخلية / placeholder (INTERNAL) — تُستبعد من تقارير الغياب (قرار الـ Owner 2026-06-06)
- مجموعات **مش تدريس حقيقي**: اختبار تحديد المستوى (`Placement Test`/`Placemnent Test`/`تحديد مستوى` بكل الإملاءات)، `Free Slots(DONOT CLOSED)`، `Hiring New Teacher`. العميل يُوضع فيها مؤقتًا ومجموعته/منسقه الحقيقي مختلف (مثال: zain tamer في «Placemnent Test»/doha بينما مجموعته الفعلية مع magdy).
- كانت **~79% من «غياب المحاضرات الأساسية»** تأتي منها وتُنسب لمنسق غلط.
- **القاعدة:** استبعدها من **كل** تقارير الغياب/العدّ عبر helper `notInternalGroup(groupExpr)` (LIKE `%free slot%` / `%hiring new teacher%` / `%placem%test%` / `%تحديد مستو%`). مطبَّق في: `/absent-list` (part1+2)، `/absent-side-list`، `/remarks-notes-main`، `/remarks-notes-zoom`، `/attendance-absence`، `/quality-employee` (العدّ + التفاصيل). (code-problems كان يستبعد Free Slots/Hiring بالفعل.) النتيجة: absent-list 13,696 → 2,892.

### مطابقة تواريخ الريمارك بالغياب (DATE + HOLIDAY)
- المتوقع: ريمارك `Attendance Main Session/Zoom Call` بتاريخ = **اليوم الذي بعد الغياب** (`nextRemarkDay`: +1، أو +2 لو خميس [الجمعة عطلة])، **ويتخطّى نطاقات `official_holidays`** (إجازة العيد). `prevLectureDay` هو العكس. تطبيع تاريخ الريمارك من `DD/MM/YYYY, ...` عبر `normRemarkDate`.

### تقارير الجودة (`/quality-employee`)
- غياب الزووم (zoom_absent): **العدّ والتفاصيل** كلاهما من **`absent_zoom_students`** (الملف المرفوع) — ليتطابقا. (قرار الـ Owner.)
- غياب الأساسية: rename-aware (يطابق الـ batch بالاسم القديم أو الجديد).

### تسليمات الأقسام (`csDeliveries.service.js`)
- شهور/مستويات العميل في صفحة قسم = **لقسم تلك الصفحة فقط** (per-dept)، وليس إجمالي رحلته عبر الأقسام. (قرار الـ Owner.) `remaining_levels = max(0, paid_months_per_dept - groups_taken)`.
- **dedup «النشطة» مقابل «غير النشطة» بالكود الأساسي للمجموعة (2026-06-13):** المجموعة الواحدة كانت تظهر في **العمودين** لأن مفتاح الـ dedup كان يقارن الاسم كامل (مع لاحقة المنسق). اللاحقة (الاسم بعد القوس) تختلف بين المصدرين لنفس المجموعة بعد تسليم منسق: `batches` تخزّن `...General1(Mariam Saad)Zainab` بينما ملف مستويات Drive (`cs_completed_levels`) يخزّن `...General1(Mariam Saad)nasreen` — فالفلتر ما كان يطابق وتتكرر (وتُحسب كمستوى مرتين). **الحل:** مفتاح `canonGroupKey(s)` = القصّ عند **أول `(`** (يشيل `(المدرّب)` + لاحقة المنسق) + إزالة المسافات، يُطبَّق على **الطرفين** قبل المقارنة. يتعامل مع اختلاف المنسق و/أو المدرّب وغياب القوس من أحد الطرفين. **متحقَّق على snapshot حيّ:** 0 تكرار متبقٍّ عبر 3619 صف، **302 عميل اتصلّح**، بلا over-dedup (بادئة تاريخ+يوم+وقت+مستوى فريدة لكل مجموعة). [نُشر — commit 1b7181d.]
- **عمودا «عدد المحاضرات» + «تاريخ أول/آخر محاضرة» لكل مجموعة نشطة (2026-06-13):** `active_groups_meta[]` (محاذٍ لترتيب `active_groups`) لكل مجموعة نشطة: (1) **عدد المحاضرات** = `COUNT(DISTINCT date||'|'||time)` من `lectures` بمطابقة `group_name`+`line`، **`session_type='main'`** (الجانبية/زوم مستبعدة)، **`status IN ('مؤكدة','مجدولة')`** = «كل المسجّلة» (قرار الـ Owner؛ DISTINCT لطيّ توائم RENAME)؛ (2) **تاريخ البداية/النهاية** = `MIN(date)`→`MAX(date)` على **نفس** المحاضرات — **من جدول `lectures` مباشرة، لا من `batches.start_date/end_date`** (قرار الـ Owner صراحةً: «تاريخ أول وآخر محاضرة موجودة في المجموعة، مش من تقرير الباتش»). `makeGroupLectureMeta()` يحسبها مع memo لكل (group,line). متحقَّق على snapshot حيّ: مطابق للاستعلام الخام 0 اختلاف/27 إدخال. [نُشر — commit b6551b8.] **تحديث 2026-06-18 (commit 64c5edc): الشيت الحالي فقط** — أُضيف `INNER JOIN (… date(MAX(synced_at)) sd … GROUP BY group_name,line) ls ON … date(synced_at)=ls.sd` فالعدّ والتواريخ يقتصروا على **أحدث استيراد للمجموعة = شيت Lecture-Main-Session الحالي**، لأن الصفوف المؤكدة القديمة المتراكمة كانت تضخّمه (Doha Tarik: 18→8، أول تاريخ 06-10→06-13). العدّ يظل per distinct `(date|time)` = جلسات الشيت (مجموعة Con خاص بجلستين/يوم تظهر 14 بحق).
- **فلاتر الصفحة + إعادة ترتيب الحساب (2026-06-13):** فلاتر جديدة على `/cs/deliveries`: `coordinator` (قائمة منسدلة؛ الـ endpoint يرجّع `coordinators[]` = المنسقون المميّزون في القسم، مطابقة بمفتاح مضغوط)، `first_from`/`first_to` (تاريخ أول محاضرة)، `last_from`/`last_to` (تاريخ آخر محاضرة)، `remaining_min`/`remaining_max` (المستويات المتبقية). **منطق التاريخ = يظهر العميل لو أي مجموعة نشطة طابقت المدى** (قرار الـ Owner). **مهم معماريًا:** الحقول القابلة للفلترة (`inactive_groups`/`active_groups_meta`/`remaining_levels`) بقت تُحسب **لكل العملاء قبل التقسيم لصفحات** (كانت لشريحة الصفحة فقط) — رخيص لأنها من خرائط جاهزة + lecture lookups memoized؛ حساب الـ pacing الثقيل (`csClientPlan`: المكتمل/تاريخ الانتهاء/متأخر) **يفضل لشريحة الصفحة فقط** (عرض لا فلترة). متحقَّق حيّ: baseline 1959 ثابت في 77ms، وكل الفلاتر صحيحة (yassmen=90، يونيو-أول=325، يوليو-آخر=291، متبقي 3-5=428). [نُشر — commit 8a47910.]
- **عدّ عملاء المنسق = زي فريق العمل + ضمّ العملاء بلا اشتراك (2026-06-18، تغيير قرار):** الـ Owner عايز عدد عملاء كل منسق في التسليمات يطابق صفحة فريق العمل (`/org-chart/customer-services`). تغييران: (1) **فلتر المنسق** بقى «أي عميل في مجموعة نشطة للمنسق» (`activeMap[phone].some(coordStrHasName)`) بدل «المنسق الرئيسي المحلول» — فالعميل في مجموعتين بمنسقين يظهر تحت الاتنين (تطابق فريق العمل)؛ والقائمة المنسدلة بقت من **كل** منسقي المجموعات النشطة في القسم. (2) **نطاق الصفحة اتوسّع**: كان «العملاء ذوو الاشتراك فقط» (قرار سابق) → بقى **اتحاد (اشتراك ∪ مجموعة نشطة)**؛ العميل بلا اشتراك يظهر بأعمدة عضوية/شهور/متبقي = `null` («—») وعلم `has_subscription=false`. كمان `realCoordOf` بيتخطّى لاحقة `--` (تعويض سيشن) لعرض المنسق الحقيقي. **الفرق الباقي البسيط مقصود:** فريق العمل يعدّ `COUNT(DISTINCT c.id)` = تسجيلات (العميل في مجموعتين لنفس المنسق يُعدّ مرتين)، والتسليمات تعدّ **الأشخاص المميّزين بالتليفون مرة واحدة** (الأصح) — مثال Magdy فريق العمل 67/تسليمات 65 (عميلان في مجموعتين)، doha 109/98 (11 عميل). متحقَّق حيّ. [نُشر — commit (deliveries).]
- **استبعاد مجموعات التعويض (2026-06-18):** `IGNORED_GROUP_PATTERNS` في `csDeliveries.service.js` اتزاد عليه `/comp[ae]ns/i` (Compensation/Compansation) و`/تعويض/` — مجموعات التعويض مش مستويات حقيقية، فتُستبعد من النشطة + المنتهية + عدّ المتبقي + قائمة/فلتر المنسقين (زي Free Slots/Grammar). متحقَّق: 0 ظاهرة عبر كل الأقسام (كانت 3 batches نشطة). [نُشر — commit 5ea114a.]
- **الـ upgrade يحلّ محل الاشتراك القديم (2026-06-18، `csIngestFinance.service.js`):** العميل اللي يعمل upgrade (مثلاً برايفت سينجل 1ش → سوبر جولد 6ش) لازم يظهر **عضوية واحدة = شهور الـ upgrade** مش (القديم + الجديد). `_markPriorSupersededByUpgrade` بيعلّم أحدث اشتراك أقدم non-ignored لنفس (phone,dept) بـ `is_ignored='superseded_by_upgrade'` لما تتسجّل معاملة `type='upgrade'`. idempotent (الـ upsert بيعيد ضبط is_ignored كل run؛ المعاملات date-ASC فالأقدم موجود؛ الأقرب-السابق فقط بلا cascade). متحقَّق على نسخة حيّة: Hussein (01066693414) Private → عضوية 1 / 6 شهور (بدل 2 / 7)؛ التأثير الكلي 7 عملاء فقط من 37 upgrade. **يسري بعد تشغيل الـ ingest المالي** («تحديث البيانات شامل»). [نُشر — commit fc68c40.]
- scoping المنسق (role=agent): يطابق `batches.coordinators` بمفتاح مضغوط (مسافات).
- شرط «Start» في Enrollment = العدد الفعلي من `enrollment_students` (لو في روستر) ≥ 7، لا `num_students` القابل للكتابة.

### Enr Groups (مجموعات الـ Enrollment) — عرض group-oriented (2026-06-21، `csEnrGroups.service.js`)
- **الغرض:** صفحة جديدة **أدمن-فقط** تحت Enrollment في السايدبار (`/subscriptions/enr-groups`) = عكس صفحة التسليمات: **صف لكل مجموعة** بدل صف لكل عميل. تعرض كل مجموعة **نشطة ابتديت** والعملاء بداخلها وتواريخ أول/آخر محاضرة. **إضافية بالكامل** (لا تلمس route/جدول/خدمة موجودة).
- **التعريفات (قرار Owner):** «نشطة» = `batches.status='نشطة'` (مع استبعاد المجموعات الوهمية بنفس `IGNORED_GROUP_PATTERNS`: Free Slots/Grammar/تعويض، ودمج التكرار بـ `canonGroupKey+line`). «ابتديت» = عندها **≥1 محاضرة أساسية مسجّلة** (`status IN ('مؤكدة','مجدولة')`) — أي محاضرة مسجّلة، مش بس مؤكدة.
- **تواريخ البداية/النهاية = أول/آخر محاضرة** من جدول `lectures` مباشرة (لا `batches`)، main فقط، **الشيت الحالي فقط** (أحدث `synced_at` للمجموعة)، `COUNT(DISTINCT date||'|'||time)` لطيّ توائم RENAME — **نفس منطق `csDeliveries.makeGroupLectureMeta` بالظبط** فالأرقام تتطابق.
- **الأعمدة:** كود المجموعة (+تاج line) · المنسق (من `batches.coordinators` يتخطّى `--`) · الطلاب (اسم+تليفون من `clients` بمطابقة group_name+line، dedup بالتليفون) · عدد الطلاب · تاريخ البداية · تاريخ النهاية · عدد المحاضرات. تبويب بالقسم (dept_type: General/Semi/Private) + بحث (كود/منسق) + pagination. الترتيب: الأحدث بداية أولًا.
- **الـ API:** `GET /api/cs/enr-groups?dept=&q=&page=&page_size=` (`requireRole('admin')`) في `cs.routes.js`. الفرونت `EnrGroups.jsx` + route متلفّف `<PrivateRoute allowedRoles={['admin']}>` + لينك أدمن-فقط في `getAdminLinks`.
- **متحقَّق على snapshot حيّ:** الإجمالي 540 مجموعة ابتديت (جينرال 62 · سيمي 229 · برايفت 249). ملاحظة: 317/540 لها منسق حقيقي في `batches`؛ 223 منسقها `--` (مجموعات لسه ماتسندتش) تظهر «—»، منها 188 لها اسم في لاحقة اسم المجموعة بعد آخر `)` (fallback مؤجَّل لقرار الـ Owner). [نُشر — commit c6b4445.]
- **فلاتر تاريخ البداية/النهاية (2026-06-21):** أُضيفت بارامترات `first_from/first_to` (على `start_date`) و`last_from/last_to` (على `end_date`) للـ endpoint + حقول نطاق-تاريخ «أول محاضرة» و«آخر محاضرة» في الصفحة (بنفس `dateInRange` صفحة التسليمات؛ كل مجموعة لها تاريخ بداية/نهاية واحد فالفحص in-range مباشر) + زرار «مسح الفلاتر». متحقَّق حيّ: سيمي 229 → أول-محاضرة في يوليو 89، آخر-محاضرة في أغسطس 42، أول-محاضرة=يوم محدد 5. [نُشر — commit 5283550.]
- **شاشة نقل المجموعة + قوائم المتابعة (2026-06-21، قرار Owner):** الضغط على صف في Enr Groups يفتح **شاشة نقل** (أدمن فقط): (1) اختيار **«المجموعة القادمة»** من المجموعات النشطة اللي لسه ماابتدتش (نفس القسم)؛ (2) **نقل** عملاء المجموعة الحالية لـ roster المجموعة القادمة (`enr_next_members`، جدول in-app)، أو **إضافة عملاء جدد** من بحث **كشف العملاء** (`cs_sales_register`)؛ (3) للعملاء **غير المنقولين**: تسجيل **مصير** — تأجيل (تاريخ+موعد+طريقة متابعة call/whatsapp/visit+ملاحظات) / عدم الرد / غير ناجح (`enr_dispositions`). النقل والمصير **متنافيان** لكل عميل (تسجيل مصير بيشيله من الروستر، والعكس). تبويب علوي تانٍ «قوائم المتابعة» يعرض القوائم التلاتة بفلاتر (بحث/تاريخ). **تعارض اتحلّ:** الـ roster مربوط أصلًا بـ `enrollment_rows` لكن الوجهة هنا مجموعة batches → استُخدم جدول in-app جديد `enr_next_members` (مفتاحه next_group_name+line). **overlay in-app فقط — لا يلمس clients/batches من Drive.** Endpoints أدمن تحت `/api/cs/enr-groups/*`: next-options · transition · sales-search · next-members (POST/DELETE) · next-roster · disposition (POST/DELETE) · dispositions. متحقَّق على snapshot حيّ (نقل/تأجيل/قوائم). [نُشر — commit 4005aa6.] **تحديث:** قائمة «المجموعة القادمة» تفلتر بالقسم **+ نفس خط** المجموعة الحالية (Ahmed Hassan→Ahmed Hassan، Dardasha→Dardasha). [commit eecd980.] **+ تصحيح المصدر (commit c28c3d6):** القائمة بقت تقرأ المجموعات بحالة `batches.status IN ('بانتظار تسجيل المتدربين','بانتظار تسجيل المحاضرات')` الحقيقية (نفس صفحات SystemReports) بدل «نشطة بلا محاضرات» — قبل التصحيح كانت بتطلّع 2 بس لمجموعة Ahmed Hassan/General؛ بعده 29 (13 بانتظار متدربين + 16 بانتظار محاضرات).
- **حالات المجموعة + عمود/فلتر «الحالة» (2026-06-21، قرار Owner):** الصفحة بتعرض المجموعات مع عمود «الحالة» وفلتر. **التصحيح الجذري (commit c28c3d6):** الحالة = **`batches.status` الحقيقي** (نفس قيم صفحات SystemReports «بانتظار...»)، **مش** محسوبة من عدد المحاضرات. القيم: `نشطة`→started · `بانتظار تسجيل المحاضرات`→waiting_lectures · `بانتظار تسجيل المتدربين`→waiting_trainees. الصفحة بتضمّ الحالات التلاتة (`status IN`). التواريخ: لو فيه محاضرات → أول/آخر محاضرة، وإلا `batches.start_date` و`end=null`. **الغلط القديم (8cd791a، مُصحَّح):** كان بيفلتر `status='نشطة'` فقط ويحسب «بانتظار» من المحاضرات → المجموعات «بانتظار» الحقيقية (status منفصل) كانت غايبة تمامًا. متحقَّق حيّ: جينرال = 76 نشطة + 18 بانتظار محاضرات + 16 بانتظار متدربين = 110. (`batches.status` كله: نشطة 570 / بانتظار متدربين 28 / بانتظار محاضرات 22.)

### سجل العملاء «كشف العملاء» (`cs_sales_register`) — موديول إدخال جديد (2026-06-20)
- **الغرض:** نقل تاب «كشف العملاء» من Operation Sheets (Google) إلى النظام = إدخال/متابعة عمليات البيع داخل النظام. **مصدر منفصل تمامًا عن Center App** (`finance_transactions`/`cs_subscriptions`) — لا يقرأ/يكتب فيها (قرار Owner). **تحويل كامل (cutover):** بعد رفع التاريخ، كل العملاء الجدد يُدخَلون من النظام والشيت يتوقف (قرار Owner) — مفيش مخالفة لقاعدة keep-old-running لأن الشيت ملف خارجي والـ Owner هو اللي يبطّل يكتب عليه بعد التأكد.
- **الجداول (في `app.js`، idempotent):** `cs_sales_register` (الأساسي، 29 عمود من الشيت + `balance` + `source`(`sheet`|`system`) + `raw_json` + audit) + `cs_sales_installments` (child، `ON DELETE CASCADE`). الأقساط **غير محدودة** (الشيت له 3 بلوكات أقساط متكررة).
- **قرارات حاسمة (متحقَّقة على CSV حيّ = 10,008 صف):**
  - **`code` = كود العميل لا العملية:** كل الأكواد المكررة (1,350) تخص **نفس العميل** بعمليات مختلفة (كورس/Refund/Delay…)، صفر تعارض أسماء. ⇒ **ممنوع فرض تفرّد الكود** في الـ API (لازم يسمح بعمليات متعددة لنفس العميل). (أُزيل حارس 409 المبدئي.)
  - **`Balance` = الرصيد المتبقي للعملية لا قسط:** عمود 48 خُزِّن على الـ parent. كان 10,002 من 10,970 «قسط» مجرد balance-only noise (غالبًا 0 على الكاش) ⇒ الأقساط الحقيقية **968 فقط** (شرط وجود القسط = أي من sales_man/months/paid_or_not/amount/pay_date/note؛ يستبعد department+balance).
- **API** (`/api/cs-sales-register`، **admin فقط** `requireRole('admin')`): `GET /list` (pagination + فلاتر + `COUNT(*) OVER()`؛ **بحث `q`: الاسم substring `%q%`، لكن الكود والموبايل prefix `q%`** — منعًا لمطابقة كود قصير جوّه رقم موبايل غير متعلّق، مثل 22364 داخل 1223643366) · `GET /options` (distinct للقوائم) · `GET /:id` (+الأقساط) · `POST /` · `PUT /:id` (replace-all للأقساط) · `DELETE /:id` (cascade) · **`POST /import`** (رفع CSV multipart، لمرة واحدة، محمي بحارس `source='sheet'` موجود + `wipe=1` لإعادة الرفع).
- **الهجرة:** منطق موحَّد في `services/salesRegisterImport.service.js` يستخدمه CLI (`scripts/import-sales-register.js`) **و** زر «رفع CSV» في الصفحة. كل صف غير فارغ يُدخَل (10,008)، 206 صف فارغ/فاصل متخطّى، `raw_json` يحفظ الأصل = هجرة أمينة 100%. الترويسة = أول صف فيه `Code`+`Client Name` (السطر الرابع).
- **الفرونت:** `ClientSalesRegister.jsx` على `/admin/sales-register` (+لينك «كشف العملاء» في السايدبار). فورم بقوائم `datalist` (اختيار موجود أو كتابة جديد) + أقساط ديناميكية + زر رفع CSV (يعرض ملخص + يطلب «استبدال» لو مرفوع قبل كده). **مهم (React Query v5):** الـ`onSuccess`/`onError` اتشالوا من `useQuery` — فورم التعديل يُملأ من `useEffect` على `data` مش `onSuccess` (الكولباك ده شغّال على `useMutation` بس). + مولّد الحقول `F` **يُنادى كدالة `{F({...})}` لا `<F/>`** (تعريفه جوه الكومبوننت، فـ`<F/>` بيعمل remount + فقدان focus كل ضغطة). **تقليص الفورم (2026-06-20، قرار Owner):** قسم «الإجماليات والخصومات» اتشال منه `offer_individual` + `chrismss_discount_ah/dar` + `refund_deduction` + `khaled_deduction` + `new_courses` + `new_prices` (بقى Total Price / Total Paid / Discount / Balance بس). **إزالة UI فقط** — الأعمدة + بياناتها المُهاجَرة موجودة في الـ DB (SALE_FIELDS بالكامل في الـ backend)، فالتعديل يحافظ على القيم القديمة، والإرجاع سهل.
- **منطق الإجماليات الجديد (2026-06-20، قرار Owner):** قسم «الإجماليات» بقى 3 خانات: **«المبلغ المدفوع من العميل»** + `Discount` + **«Balance» (تلقائي read-only)**. اتشال `Total Price` و`Total Paid In Same Months` من الفورم (أعمدتهم في الـDB لسه). القاعدة الموحَّدة (`calcPaidBalance`، **مُصحَّحة 2026-06-20**): **«المبلغ المدفوع من العميل» = الدفعة الأولى/الكاش (`total_paid_same_month`، editable دايمًا)**، و**«إجمالي المدفوع» (تلقائي) = الدفعة الأولى + مجموع الأقساط** (الدفعة الأولى مخزّنة في عمود مستقل عن بلوكات الأقساط، فتُضاف مش تُستبدل — كان bug: صف 16103 الدفعة الأولى 1000 + قسط 990 كان بيعرض 990 بس). **Balance = (السعر الفعّال − الخصم) − إجمالي المدفوع**. **الخصم (`discountAmount`)** يقبل **نسبة** (`"10%"` → price×%) **أو مبلغ** (`"1000"`)؛ يُخزَّن خام نصًّا (`discount` للعادي / `offer_individual` للترقية، كلاهما TEXT) ويُعاد تفسيره؛ يُؤخذ بالـ`Math.abs`. عند الحفظ: المدفوع الأول في `total_paid_same_month` والـ balance في `balance`. متحقَّق: 16103 (1990 مدفوع، رصيد 0)، 22364 ترقية (7750، رصيد 0)، كاش (سليم).
- **الترقية «Upgrade» = العضوية النهائية، رصيد واحد (2026-06-20، قرار Owner — التصحيح النهائي):** العميل اللي يعمل upgrade **عضويته النهائية هي الباقة الجديدة**، والكورس القديم مجرد «بدأ بـ». checkbox «العميل عمل ترقية» في هيدر «بيانات العملية» (`isUpgrade` يُكتشف من `new_courses` مملوء أو `noted2='Upgraded'`). **لما `isUpgrade`:** الكورس الرئيسي = `new_courses` (dropdown عضويات + يملأ `new_prices` تلقائي per-brand)، السعر = `new_prices`، + حقل «بدأ بـ» = `courses` (الأصلي). **كل المدفوع يتجمّع على العضوية الجديدة:** «إجمالي المدفوع» = مدفوع الاشتراك (`total_paid_same_month`) + مجموع الأقساط (دفعات الترقية). الخصم = `offer_individual` (بدل `discount`؛ مبلغ/% بالـ`Math.abs` — المُهاجَر مثل `"-250"`). **رصيد واحد: `balance = (new_prices − خصم) − إجمالي المدفوع`** (`calcPaidBalance`). متحقَّق على 22364: العضوية 6L PAC 2P، إجمالي مدفوع 7750، **رصيد = 8000 − 250 − 7750 = 0**. (النموذج الأول «رصيد منفصل للترقية» كان فهمًا غلط واتشال.)
- **تنظيف القيم + قوائم ثابتة (2026-06-20):** توحيد البراند وحالة الدفع (قرار Owner): (1) **migration لمرة واحدة idempotent** في `app.js`: `pages` `Daradasha`+`daradasha`→**`Dardasha`** (370؛ **`Daradasha AUE` براند منفصل يفضل**)، و`paid_status`→ 3 قيم بس **`Paid`** (9,987) / **`Not Paid`** (21) / **`Fake`** (5) (الـ2 الفاضيين يفضلوا)؛ (2) **قوائم `<select>` ثابتة** في الفرونت للبراند (`BRANDS`) وحالة الدفع (`PAID_STATUSES`) — فورم + فلاتر — تمنع إدخال variant جديد؛ (3) **normalizer دفاعي** في الـ backend (`normPages`/`normPaid` في `saleValues`) عند POST/PUT. متحقَّق على snapshot حيّ.

### العضويات وأسعارها (`cs_membership_prices`) — تسعير يدوي per-brand (2026-06-20)
- **تبويب جوه صفحة «كشف العملاء» (مش صفحة منفصلة) — قرار Owner صريح:** المستخدم طلبها «بجانب قائمة العمليات» داخل نفس الصفحة. الـ`ClientSalesRegister.jsx` فيه `view` toggle: تبويبان «قائمة العمليات» / «العضويات وأسعارها». المحتوى = `MembershipPricesSection.jsx` (section بلا PageHero). **لا يوجد لينك سايدبار ولا route مستقل** (اتشالوا بعد ما اتعملوا بالغلط أول مرة). [[feedback_no_changes_without_approval]] — اتنفّذ بالظبط زي ما اتوصف.
- **سعران منفصلان لكل عضوية**: `price_ahmed_hassan` + `price_dardasha` (أسعار Ahmed Hassan تختلف عن Dardasha — قرار Owner). جدول `cs_membership_prices` (`code` UNIQUE) في `app.js`.
- API `/api/membership-prices` (**admin فقط**): `GET /list?q` · `POST /` (409 لو الكود مكرر) · `PUT /:id` · `DELETE /:id` · **`POST /seed`** (يملأ من `DISTINCT courses` في `cs_sales_register` بـ `INSERT OR IGNORE` = يضيف الجديد بس، فحذف عضوية وإعادة الـ seed ما يرجّعهاش) · **`POST /prune-no-ah`** (body `{keep:[...]}`: حذف جماعي لكل عضوية `price_ahmed_hassan` فاضي ما عدا أكواد `keep`؛ **يمسّ `cs_membership_prices` فقط — قائمة العمليات `cs_sales_register` لا تتأثر إطلاقًا**). متحقَّق حيّ: seed=198، إعادة seed=0، CRUD+dup-guard، prune (keep=`Refund`,`Revision`) حذف 181 وأبقى 17 و**العمليات 10,015→10,015 بلا تغيير**. زر «حذف بدون سعر Ahmed Hassan» في تبويب العضويات (confirm يعرض العدد).
- **الربط التلقائي بالكود (2026-06-20):** في فورم «كشف العملاء» حقل **«الكورس (Courses)»** بقى **`<select>` ثابت من `cs_membership_prices`** (مش datalist من العمليات) — قرار Owner. واختيار عضوية (أو تغيير البراند) **يملأ `price` تلقائيًا**: `Ahmed Hassan`→`price_ahmed_hassan`، `Dardasha`+`Daradasha AUE`→`price_dardasha`، باقي البراندات (Go English/Work Shop) → يدوي. التطبيق عبر `applyCourse`/`applyPages` (onChange override في `F`) **مش useEffect** — عشان فتح صف للتعديل ما يكتبش فوق السعر المحفوظ. قيمة الكورس القديمة (لو كود محذوف من العضويات) تظل ضمن الخيارات (`courseOptions`) فلا تُفقد.

### أمان
- أي endpoint يأخذ قيمًا من `req.query`/`req.body` في SQL لازم **parameterized** (`?` + bind). (مثال: ثغرة `GET /api/team` التي أُصلحت.)

### منح صفحة لمستخدم بدون تغيير دوره (`extra_pages`) — قرار Owner 2026-06-08
- عمود `extra_pages` (CSV من مفاتيح الصفحات) على جدول `users`. يسمح للأدمن يفتح صفحة محددة لـ agent **من غير ما يحوّله أدمن**.
- المفاتيح الحالية: `occupancy-trainers` (الإشغال والمدربين، 4 صفحات) + `cs-deliveries` (تسليمات الأقسام → `/subscriptions/cs-department`) + `cs-enrollment` (Enrollment → `/subscriptions/enrollment`). تُضاف في `GRANTABLE_PAGES` (UserManagement) + الـ grantedLinks (Sidebar). الواجهة قائمة منسدلة "صفحات إضافية" متعددة الاختيار.
- صفحات الـ CS (`cs-deliveries`/`cs-enrollment`) موجودة أصلًا على `/subscriptions/*` (محايد للدور) فالمنح = إظهار الرابط في الـ Sidebar بس (مفيش route جديد).
- **مهم:** الصفحات الممنوحة متركّبة على مسار محايد `/reports/*` (الأب = `<PrivateRoute><AppShell/></PrivateRoute>` = أي مستخدم مسجّل) مش تحت `/agent` — لأن `/agent` مقفول على agent/leader/admin وكان بيطرد المستخدمين بأدوار تانية (enrollment/CS) لـ home بتاعهم. كل صفحة محمية بـ `requirePage`.
- backend `/api/reports` أصلًا مفتوح لكل الـ agents (`requireRole('agent')`) → المنح **frontend-only** (مفيش تغيير صلاحية على السيرفر). القيمة بتيجي للفرونت من JWT payload (login/refresh) لأن `/me` و login response مش بيرجّعوا الحقل للـ Sidebar.
- الحارس: `PrivateRoute requirePage="..."` (غير الأدمن لازم المفتاح في `extra_pages`)؛ الـ Sidebar بيظهر قسم ممنوح؛ UserManagement فيه multi-select "صفحات إضافية".

---

## 4) الأخطاء المتكررة في البيانات (فئات الجذور) — افحصها في أي تقرير

| الفئة | الوصف | الحل المعتمد |
|---|---|---|
| RENAME | قديم في الغياب / جديد في المحاضرات+batches | `currentGroupNameExpr` / `effectiveGroupNameAtDate` + REPLACE مسافات |
| ENDED | تختفي من batches | `LEFT JOIN` + `coordinator_history` + COUNT(DISTINCT) |
| NAME-KEY | اسم مختلف عبر الجداول | مفتاح مضغوط؛ اعرض المسجّلين في الفريق فقط؛ استبعد `--` |
| STATUS | `إنتهت`/`غير منتهية` عربي | لا تفترض إنجليزي؛ `إنتهت` = مغلق |
| PHONE | فرق الصفر البادئ | `c.phone=a.phone OR '0'\|\|a.phone OR a.phone='0'\|\|c.phone` |
| SPACES | مسافات في أسماء المجموعات | `REPLACE(group_name,' ','')` الطرفين |
| OVERLAP | جمع جلسات متداخلة | `mergeIntervalsMinutes` / COUNT(DISTINCT canonical) |
| HOLIDAY | فجوة العيد في مطابقة التواريخ | `nextRemarkDay`/`prevLectureDay` تتخطّى `official_holidays` |
| INTERNAL | مجموعات placeholder (Placement Test/Free Slots/Hiring) | استبعدها عبر `notInternalGroup(groupExpr)` من كل تقارير الغياب |

---

## 5) سجل الإصلاحات المنفّذة (audit 2026-06-05/06) — ~27 إصلاح، كلها على main

> راجع `git log` و `Quality_System_Data/system_audit_2026-06-05.md` للتفاصيل الكاملة بالأرقام.

- **إشغال المدربين:** المحجوز = الوقت المشغول (دمج المتداخل) في الـ heatmap والملخص (المرتبات) — كان يصل 246%/350% وهمي.
- **تقارير الجودة:** كانت واقعة 500 (متغير `activeDept`) → 200؛ غياب الأساسية rename-aware (740→1186)؛ zoom_absent من الملف المرفوع (الشارة=التفاصيل).
- **عداد الريماركات المفتوحة:** 24,295 → 147 (`إنتهت` = مغلق)؛ Remarks Monitor «resolved» = `إنتهت`.
- **الغياب/الريماركات:** غياب الزووم المعاد تسميته/المنتهي رجع (كان يسقط 2207)؛ القسم «—» يُسترجع من coordinator_history بمطابقة اسم مرنة؛ اسم منسق مكرر «doha, doha» → DISTINCT؛ استبعاد «--» وغير المسجّلين.
- **الحضور/الغياب:** المنسق المشترك يُحسب مرة (الأقدم)؛ `/segments` يطابق الصف؛ fallback لقسم الفريق عند غياب dept_history.
- **(2026-06-06) `employmentWindowFilter` صار rename/ended-aware:** كان يربط lookup الـ coordinator_history بـ `b.group_name` (alias batches)، فالمجموعات المنتهية/المعاد تسميتها (b=NULL في LEFT JOIN) كانت تُسقَط كلها → مودال «تفاصيل الغياب» في تقرير الحضور/الغياب يظهر **عرض 0** للمنسق رغم وجود عدّ (مثال yassmen: 251 مقابل 0). الإصلاح: يفتاح على مجموعة الحدث نفسها (`a/l`) عبر `effectiveGroupNameAtDate` — زي `coordFilterAtDate`. أُصلحت الدالة + 4 استدعاءات (absent-list ج1/ج2، absent-side-list، absent-zoom). متحقَّق: المودال 0 → 246/296.
- **(2026-06-06) توحيد مصدر غياب الزووم في `/attendance-absence`:** الجدول كان يحسب `zoom_absent` من جدول `lectures` (خانات الجلسات الجانبية بلا حضور) — وده يضخّم بشدة لأن حضور الزووم لا يُسجَّل في `lectures.attendance` بل في الملف المرفوع. بقى يستخدم **`absent_zoom_students` (الملف المرفوع)** لمّا تتاح — نفس مصدر الداشبورد و`/absent-side-list` وقرار 2026-06-06 — مع fallback لصيغة lectures القديمة لو لا ملف للّاين. الأثر: yassmen zoom_absent 1631→**334**، إجمالي الزووم ~10277→**3611**.
- **(2026-06-06) `dateAwareCoord` في `/attendance-absence` صار rename-aware:** كان ينسب الأحداث للمنسق باستخدام الاسم الخام (`${alias}.group_name`) في lookup الـ `coordinator_history`، بينما القوائم drill-down (`coordFilterAtDate`) rename-aware — فاختلفت الأرقام (yassmen: جدول 334/251 مقابل مودال 296/246). أُصلح بلفّ الاسم في `effectiveGroupNameAtDate` (نفس قاعدة بقية المطابقات). النتيجة: **العدّ = التفاصيل بالظبط** (yassmen 246/296، shrouk gamal 128/309، doha 177/246 — كلها جدول=مودال). يطبّق على كل أرقام الجدول (main_expected/absent + zoom_expected/absent).
- **(2026-06-06) شارة «⚠ موظف آخر» في ريماركات المحاضرات الأساسية:** كانت تظهر في جدول الجلسات الجانبية فقط (`SystemReports.jsx` عمود المسؤول) — لمّا يكون منفّذ الريمارك ≠ منسق المجموعة. أُضيف نفس الشرط بالظبط لجدول المحاضرات الأساسية للاتساق (frontend فقط؛ `/remarks-notes-main` يرجّع `coordinators/dept_type/has_remark/assigned_to` أصلًا). نشر Vercel — commit d9bb863.
- **عدّ الداشبورد:** main/side/zoom = LEFT JOIN + COUNT(DISTINCT canonical) (يضمّ المنتهية، يمنع ازدواج التكرار) — main مايو 1748→5077.
- **(2026-06-06) تصليب RENAME ضد تلوّث `group_renames`** (تفاصيل في القسم 3): `effectiveGroupNameAtDate` بقاعدة `MIN(renamed_on)` + `currentGroupNameExpr` يرسو على `batches`. أثره: غياب المنسقين المعاد تسمية مجموعاتهم رجع (RadwaGamal 0/0→18/19)، وعدّ المحاضرات لكل منسق بطّل يتضاعف. **تأثير على رقم معاير:** الـ COUNT(DISTINCT) العام يقلّ (main 2026-05-01..06-06: ~5567→4643، side ~9746→7992) لأنه كان يضخّم بسبب فشل دمج التوائم — تم التحقّق أن التقليل دمج توائم حقيقية فقط (عينة 8/10 rename-linked، بلا over-dedup). [نُشر — commit 1f759dd.]
- **(2026-06-06) إصلاح جذر تلوّث `group_renames` (WRITE-side):** في `sync.service.js` (كشف التسمية) أُضيف حارسان قبل `INSERT`: (1) **idempotent** — تخطّي لو نفس `(old,new,line)` مسجّل (يحافظ على أقدم `renamed_on`)؛ (2) **منع العكس** — تخطّي لو الحافة العكسية `(new→old)` مسجّلة (أول اتجاه/التسمية الحقيقية يكسب، لا دورات A↔B). السبب: `UNIQUE(new_group_name,line,renamed_on)` يشمل التاريخ فكل سينك يعيد ختم صف جديد، و `DELETE FROM batches WHERE line=?` ثم إعادة الإدخال من ملف Drive قديم يقلب الاسم ويسجّل العكس. **+ migration لمرة واحدة (idempotent) في `app.js`** ينظّف الموجود: يبقي أقدم `renamed_on` لكل `(old,new,line)`، ثم يحذف الجانب الخطأ من كل زوج معكوس (الذي `new_group_name` بتاعه ليس الاسم الحيّ في `batches`؛ وإن تعذّر يبقي الأقدم). نسخة احتياطية لمرة واحدة في `group_renames_backup_20260606`. **متحقَّق على اللايف:** 1930→674 صف، صفر تكرارات/عكوسات، idempotent؛ الغياب ثابت (18/19)، وعدّ main العام يقلّ (4923→4736 على نفس الـ snapshot = ازدواج أقل). **حدّ معروف:** مجموعة رجعت فعلًا لاسمها القديم لن تُسجَّل (مقايضة ضد التلوّث)؛ وملف Drive قديم قد يقلب اسم batches (مصدره محتوى الملفات).
- **ابحث عن مدرب:** بلا INNER JOIN batches (منع الحجز المزدوج).
- **code-problems:** تخطّي فحص آخر-تاريخ للمجموعات المكثفة؛ استبعاد «Free Slots»/«Hiring New Teacher».
- **الفريق:** `coordinator_type` صار قابلًا للتعديل؛ مزامنة `member_name` عند تغيير الاسم؛ **إصلاح ثغرة SQL injection** في `GET /api/team`.
- **التسليمات/Enrollment:** مطابقة اسم المنسق بمسافات؛ اقتراح المدرّس بلا batches + استبعاد تواريخ مستقبلية؛ شرط Start على الروستر الفعلي؛ شهور per-dept.
- **التواريخ:** مطابقة الريمارك تتخطّى إجازة العيد.
- **data-export:** أضيف `/db` (snapshot حيّ) + استعلامات تشخيص — قراءة-فقط، gated.

**بنود مؤجَّلة (ليست أخطاء كود — قرار/تنظيف بيانات من الـ Owner):**
- حذف صفوف المحاضرات المكررة فعليًا (مرفوض — خطر فقد بيانات؛ التقارير تتعامل معها).
- أسماء منسقين غير موجودة في الفريق (Ahmed Abdelal/Asmaa/Samah/MohamedDardasha) — تُتجاهَل حتى يضيفها الـ Owner.
- 286 غياب زووم بلا جلسة مطابقة (نقص بيانات في ملفات Drive — قائمة في `Quality_System_Data/غياب_زووم_بلا_جلسة.xlsx`).
- backfill أحداث resolved التاريخية (الكود متصلّح للجديد فقط — بقرار الـ Owner).
- **توائم محاضرات باسم مدرّب مختلف:** صفّان لنفس (group+date+time) لكن `trainer` مختلف (مثال May_17: «Esraa Hani» مقابل «Manar Abdulhameed») يظلّان جلستين لأن `sessKey` يشمل المدرّب عمدًا — مشكلة جودة بيانات منفصلة، لا تُدمج تلقائيًا (تغيير `sessKey` يتعارض مع قرار 2026-06-06).
- ~~**جذر تلوّث `group_renames`:** إصلاح `driveSync`~~ → **تم (2026-06-06)** — انظر سجل الإصلاحات أدناه.

---

## 6) خريطة التقارير الرئيسية (endpoint ↔ شاشة)

| الشاشة (frontend) | الـ endpoint(s) |
|---|---|
| تقارير خدمة العملاء `SystemReports.jsx` | `/reports/dashboard`, `/absent-list`, `/absent-side-list`, `/remarks-notes-main`, `/remarks-notes-zoom`, `/remarks-notes-options`, `/remarks-list`, `/remarks-categories`, `/code-problems` |
| الحضور والغياب `AttendanceAbsenceReport.jsx` | `/attendance-absence`, `/attendance-absence/segments` |
| تقارير الجودة `QualityReports.jsx` | `/quality-employee`, `/quality-employee/details`, `/quality-snapshot*` |
| إشغال المدربين `TrainerUtilizationDashboard.jsx` / `TrainerUtilization.jsx` | `/trainer-utilization`, `/trainer-utilization-summary` |
| ابحث عن مدرب `TrainerAvailabilityFinder.jsx` | `/find-available-trainer` |
| سجل عمل المدربين `TrainerWorkHistory.jsx` | `/trainer-work-history` |
| الفريق والهيكل `TeamPage.jsx`/`OrgChart.jsx` | `/api/team*`, `/api/org-chart*` |
| Remarks Monitor `RemarksMonitor.jsx` | `/api/remarks-monitor*` (service: `remarksMonitor.service.js`) |
| تسليمات الأقسام / Enrollment / التوزيع | `/api/cs*`, `/api/enrollment*`, `/api/distribution*` (services: `csDeliveries`, `csEnrollment`, `csClientPlan`...) |
| Enr Groups (مجموعات الـ Enrollment، أدمن) `EnrGroups.jsx` | `/api/cs/enr-groups` (service: `csEnrGroups`) |
| المرتبات / المالية | `/api/trainer-salaries*`, `/api/finance*`, `/api/clients-finance*` |
| سجل العملاء (كشف العملاء) `ClientSalesRegister.jsx` | `/api/cs-sales-register/{list,options,import,:id}` (service: `salesRegisterImport`) |
| العضويات وأسعارها (تبويب جوه كشف العملاء) `MembershipPricesSection.jsx` | `/api/membership-prices/{list,seed,:id}` |

---

## 7) ملاحظات تشغيل
- التحقّق من syntax: باك `node --check <file>`؛ فرونت `npx --no-install esbuild <file.jsx> --loader:.jsx=jsx --bundle=false --format=esm --outfile=NUL`.
- النشر يستغرق 1-3 دقائق (Railway native rebuild). افحص الصحة: `GET /api/health` (200).
- رسائل commit تنتهي بـ `Co-Authored-By: Claude ...`.
- **الأداء (2026-06-18):** `better-sqlite3` **متزامن** → أي استعلام تقرير تقيل بيقفل الـ event loop لكل المستخدمين. التقارير التقيلة (remarks-notes، absent-list، quality-employee، attendance-absence: 13-54 ثانية) متغطّاة بكاش سيرفر في `utils/reportCache.js` (TTL 60ث، مفتاح متضمّن هوية المستخدم id/role/line، يُبطَّل عند أي مزامنة عبر `bustReportCache()` في `syncFile`) مطبَّق على allowlist read-only فقط (مش code-problems ولا الإشغال/المرتبات). + pagination single-pass (`COUNT(*) OVER()`) بدل تشغيل الاستعلام مرتين. **لا تكاش endpoints تفاعلية أو تغذّي المرتبات.** إعادة هيكلة الاستعلامات (correlated subqueries) + worker threads اتأجّلت عمدًا (خطر > فائدة بعد الكاش). تفاصيل: [[project_reports_perf_cache]]. (commit b0831e6)

> **تذكير أخير:** عند أي طلب تعديل، طابقه على القسم 3 أولًا. لو فيه تعارض → نبّه الـ Owner واطلب تأكيدًا، ثم حدّث هذا الملف بالتاريخ.
